{
  "type": "module",
  "name": "ors-benchmark",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "swc src -d dist",
    "build-dev": "npm run build && NODE_ENV=development node --inspect dist/index.js",
    "dev": "nodemon --ext ts --exec \"npm run build-dev\"",
    "request": "npm run build && NODE_ENV=production node dist/index.js",
    "start": "npm run build && NODE_ENV=production node dist/index.js && node dist/getAverage.js",
    "cleanup": "rm -rf output/*",
    "average": "npm run build && node dist/getAverage.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.44",
    "@types/node": "^18.15.11",
    "nodemon": "^2.0.22",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "@types/jsonfile": "^6.1.1",
    "axios": "^1.3.4",
    "dotenv": "^16.0.3",
    "jsonfile": "^6.1.0",
    "parse-kml": "^1.0.1",
    "performance-now": "^2.1.0"
  }
}
